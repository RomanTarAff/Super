name: Buy Manual

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Environment
        required: true
        type: choice
        options:
          - develop
          - test

jobs:
  api-tests:
    uses: ./.github/workflows/buy.yml
    with:
      environment: ${{ github.event.inputs.environment }}