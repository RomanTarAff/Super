name: API Smoke Manual

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Environment
        required: true
        type: choice
        options:
          - develop
          - test

jobs:
  api-tests:
    uses: ./.github/workflows/apiSmoke.yml
    with:
      environment: ${{ github.event.inputs.environment }}